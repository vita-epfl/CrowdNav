from setuptools import setup


setup(name='gym_crowd',
      version='0.0.1',
      install_requires=['gym', 'numpy', 'matplotlib']
)

setup(name='dynav',
      version='0.0.1',
      install_requires=['gym']
)